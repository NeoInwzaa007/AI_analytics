version: "3.8"

services:
  db:
    image: postgres:15
    restart: always
    env_file:
      - ../.env.local
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  api:
    image: ai_analytics_api
    build: .
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ../.env.local
    volumes:
      - ./:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - DB_HOST=db
    depends_on:
      - db

volumes:
  postgres_data:
